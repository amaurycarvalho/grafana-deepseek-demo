apiVersion: 1

apps:
  - type: 'grafana-llm-app'
    disabled: false
    jsonData:
      openai:
        provider: openai
        url: http://mcp-bridge:3001
        organizationId: $OPENAI_ORGANIZATION_ID

    secureJsonData:
      openAIKey: $OPENAI_API_KEY
